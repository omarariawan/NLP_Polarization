# NLP Polarization Detection using Roberta Large Model

Run this using google colab

```shell
!pip install -r requirements.txt
!pip install --upgrade "transformers>=4.30.0"
```
Lastly just run the python code
